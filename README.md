# Python Factorial Program 🐍

A simple Python program to calculate the **factorial of a number** using a `for` loop.

## 📌 About

This project is a beginner-friendly Python program that:

- Takes an integer as input
- Checks whether the number is negative
- Calculates the factorial using a loop
- Handles invalid input using `try-except`

## 🧮 What is Factorial?

The factorial of a positive integer `n` is the product of all positive integers from `1` to `n`.

For example:

```text
5! = 5 × 4 × 3 × 2 × 1
   = 120
```

Also:

```text
0! = 1
```

## 🚀 How to Run

1. Make sure Python is installed on your computer.
2. Clone this repository:

```bash
git clone https://github.com/your-username/python-factorial.git
```

3. Open the project folder:

```bash
cd python-factorial
```

4. Run the Python file:

```bash
python factorial.py
```

## 💻 Example

```text
Enter a number: 5
Factorial of 5 = 120
```

For invalid input:

```text
Enter a number: abc
Invalid input. Please enter an integer.
```

## 📂 Project Structure

```text
python-factorial/
│
├── factorial.py
└── README.md
```

## 🛠️ Concepts Used

- Python Functions
- User Input
- Type Conversion
- `if-elif-else`
- `for` Loop
- `range()`
- Exception Handling (`try-except`)
- f-strings

## 🎯 Purpose

This project was created as part of my journey to learn **Python programming** and understand how C programs can be implemented using Python.

## 📄 License

This project is open-source and available for learning and educational purposes.
